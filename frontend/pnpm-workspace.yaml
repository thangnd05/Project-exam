# react-simple-maps kéo theo d3-color 2.x dính ReDoS; 3.x cùng API nên ép cả cây lên.
overrides:
  d3-color: ^3.1.0

allowBuilds:
  '@parcel/watcher': true
  esbuild: true
  # resolver native của eslint-import-resolver-typescript (kéo theo eslint-config-next)
  unrs-resolver: true
