// Ảnh tĩnh (đã lấy .src nên là chuỗi URL) và icon SVG inline.
// `styles/` không gom vào đây: SCSS được import trực tiếp trong từng component/layout.
export { default as images, SearchIcon, name } from './images';
